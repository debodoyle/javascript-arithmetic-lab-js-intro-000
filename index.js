function add(a, b) {
  return a + b;
}

const subtract = (a, b) => {
  return a - b;
}

function divide(a, b) {
  return a / b;
}

const multiply = (a, b) => {
  return a * b;
}

function inc(n) {
  return n += 1;
}

add(5, 4);
subtract(3, 2);
divide(6, 2);
multiply(5, 3);
inc(2);